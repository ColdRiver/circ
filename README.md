To run with RL

change dir to RL directory, and set PYTHONPATH to RL directory

run python train.py

To run with baseline

change dir to baseline directory, and set PYTHONPATH to baseline directory

run python optimizer.py
